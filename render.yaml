services:
  - type: web
    name: telaviva-homolog
    runtime: static
    rootDir: preview
    buildCommand: echo "TelaViva static homologation preview"
    staticPublishPath: .
    autoDeployTrigger: off
